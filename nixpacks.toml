[[providers]]
name = "python"
version = "3.11"

[phases.setup]
nixPkgs = ["ffmpeg", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[phases.start]
cmd = "python run.py"
